# The API origin the app talks to. Inlined into the bundle at build time
# (that is what the EXPO_PUBLIC_ prefix means) — never put a secret here.
#
# A phone cannot reach your laptop's localhost, so on a real device this must
# be your machine's LAN address, not http://localhost:3000.
EXPO_PUBLIC_API_URL="http://localhost:3000"
